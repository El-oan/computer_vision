

things to add to the YOLO pipeline:
- Kaiming (He) Initialization that divides the starting coefficients by sqrt(2/n) to keep variance constant related to amount of coefficients
- ResNet architecture : f(x) = conv(x) + x so the network can bypass useless layers


